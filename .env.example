# ============================================================================
# CygnusWealth RPC Provider Configuration
# ============================================================================
# Copy this file to .env.local and fill in your API keys.
# All keys are optional — missing keys gracefully skip that provider tier
# and fall through to public endpoints.
#
# Vite requires the VITE_ prefix for client-side env var access.
# ============================================================================

# --- RPC Provider API Keys ---------------------------------------------------
# These are read by buildRpcProviderConfig() to construct per-chain fallback
# chains. Priority order: Alchemy > Infura > dRPC > QuickNode > Public.

# Alchemy (EVM + Solana) — https://dashboard.alchemy.com/
VITE_CYGNUS_RPC_ALCHEMY_KEY=

# dRPC (EVM) — https://drpc.org/
VITE_CYGNUS_RPC_DRPC_KEY=

# Helius (Solana primary) — https://dev.helius.xyz/
VITE_CYGNUS_RPC_HELIUS_KEY=

# Infura (EVM) — https://app.infura.io/
VITE_CYGNUS_RPC_INFURA_KEY=

# QuickNode (EVM + Solana) — https://dashboard.quicknode.com/
VITE_CYGNUS_RPC_QUICKNODE_KEY=

# --- Network Environment -----------------------------------------------------
# Override the auto-detected environment. Valid: production | testnet | local
# VITE_NETWORK_ENV=production

# --- Per-Chain RPC Overrides (legacy, optional) ------------------------------
# Override a specific chain's RPC URL directly. Takes precedence over provider
# config when used via getCustomRpcUrl().
# VITE_RPC_1_HTTP=https://your-custom-eth-rpc.example.com
# VITE_RPC_1_WS=wss://your-custom-eth-ws.example.com
# VITE_RPC_137_HTTP=https://your-custom-polygon-rpc.example.com

# --- CI Mode ------------------------------------------------------------------
# Set to 'true' in CI pipelines to auto-select testnet environment.
# VITE_CI=true
